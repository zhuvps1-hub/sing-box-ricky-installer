# iWAN Gateway v7.1 部署与安全指南

v7.1 将面板拆分为两个进程：

- `iwan-gateway.service`：以专用非特权用户运行，只监听 `127.0.0.1:8088`。
- `iwan-gateway-helper.service`：以 root 运行，只接受本机 Unix Socket 请求，并通过 `SO_PEERCRED` 校验调用者 UID。

浏览器提交的 JSON 不会直接复制进 sing-box。Web 进程先转成稳定配置模型，helper 再执行校验、备份、原子替换、重启、健康检查和失败回滚。

## 1. 安装或升级

稳定分支合并后执行：

```bash
curl -fsSL https://raw.githubusercontent.com/zhuvps1-hub/sing-box-ricky-installer/main/install-panel-v71.sh \
  -o /root/install-panel-v71.sh
bash /root/install-panel-v71.sh
```

安装器会验证：

1. 固定 Ed25519 公钥的 SHA-256 指纹。
2. `panel-release.json` 的 Ed25519 签名。
3. 每个发布文件的 SHA-256。
4. 每个带 `git_blob` 文件的 Git Blob SHA-1。
5. 新版本独立自检和无旧版本栈依赖。
6. 两个 systemd 服务启动后的 `/healthz`。

健康检查失败时，`current` 软链接会自动切回上一个版本。

## 2. 防火墙

面板端口 `8088` 不应向公网开放。只开放：

```text
22/tcp     管理 SSH，最好限制来源 IP
80/tcp     ACME/HTTP 跳转
443/tcp    HTTPS 面板
```

使用 UFW 的示例：

```bash
ufw deny 8088/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw status numbered
```

云厂商安全组也必须同步关闭 `8088/TCP`。

## 3. Caddy HTTPS（推荐）

复制模板：

```bash
cp deploy/caddy/Caddyfile.example /etc/caddy/Caddyfile
```

把 `panel.example.com` 替换为实际域名，然后执行：

```bash
caddy validate --config /etc/caddy/Caddyfile
systemctl reload caddy
```

Caddy 会自动申请和续签证书，并把真实协议与来源地址传给面板。

## 4. Nginx HTTPS

复制模板：

```bash
cp deploy/nginx/iwan-gateway.conf.example /etc/nginx/sites-available/iwan-gateway.conf
ln -s /etc/nginx/sites-available/iwan-gateway.conf /etc/nginx/sites-enabled/iwan-gateway.conf
```

修改域名和证书路径，再执行：

```bash
nginx -t
systemctl reload nginx
```

## 5. 服务与日志

```bash
systemctl status iwan-gateway-helper --no-pager
systemctl status iwan-gateway --no-pager
journalctl -u iwan-gateway-helper -u iwan-gateway -n 200 --no-pager
curl -fsS http://127.0.0.1:8088/healthz
```

审计日志默认位于：

```text
/var/lib/iwan-gateway/audit.jsonl
```

日志会屏蔽密码、令牌、Authorization、Shadowsocks 链接等敏感内容，并进行容量轮换。

## 6. 回滚

回滚到最近一个历史版本：

```bash
bash /root/install-panel-v71.sh --rollback
```

回滚到指定版本目录名：

```bash
bash /root/install-panel-v71.sh --rollback 7.1.0-abcdef123456
```

版本目录位于：

```text
/opt/iwan-gateway/releases/
```

配置备份位于：

```text
/etc/sing-box/backups/
/etc/mosdns/backups/
```

## 7. 发布私钥管理

仓库只保存 Ed25519 公钥。发布私钥必须：

- 不提交到 GitHub。
- 存放在离线密码库、硬件安全设备或受保护的 CI Secret 中。
- 只用于签署规范化后的 `panel-release.json`。
- 泄露时立即生成新密钥、更新仓库公钥和安装器固定指纹，再发布密钥轮换版本。

签名命令：

```bash
python3 tools/release_manifest.py sign panel-release.json /secure/path/release-private.pem \
  --key-id iwan-release-2026-01
python3 tools/release_manifest.py verify panel-release.json release/iwan-release-ed25519-public.pem
```

## 8. 上线前检查清单

```text
[ ] GitHub Actions 全部通过
[ ] 发布清单签名通过
[ ] Raw/CDN 固定提交下载测试通过
[ ] 8088 未向公网开放
[ ] HTTPS 证书有效
[ ] /healthz 返回 ok=true
[ ] sing-box check 通过
[ ] root helper 与非 root Web 用户正确
[ ] 完成一次真实升级和一次回滚演练
[ ] 发布私钥已离线备份
```
